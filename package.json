{
  "name": "ecommerce-platform",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "backend:dev": "npm run start:dev --workspace=apps/backend",
    "backend:seed": "npm run seed --workspace=apps/backend",
    "storefront:dev": "npm run dev --workspace=apps/storefront",
    "admin:dev": "npm run dev --workspace=apps/admin -- -p 3001"
  }
}
